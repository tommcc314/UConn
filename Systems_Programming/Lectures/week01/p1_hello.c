// Include stdio.h to use the function printf
#include <stdio.h>

// This is where the execution of the program would always begin
int main(int argc, char* argv[]) {
	printf("Hello world!\n"); //prints Hello world on stdout. \n is the new line character
	return 0; //return the control from the function main with status 0
}
