#include <stdio.h>
#include <math.h>

double halleys(double x, double epsilon) {
	/* function to compute halleys sequence
	 * s = prev_s * (prev_s * prev_s + 3 * x) / (3 * prev_s * prev_s + x)
	 * calculate s as long as |s - prev_s| exceeds a threshold epsilon
	 * when the difference is less than epsilon, return the value */
}

int main() {
	/* take as input a number x for which the sqrt needs to be calculated
	 * take as input the threshold epsilon that marks the convergence of our calculation
	 * pass x and epsilon as arguments to halleys method and store and print the result */
}
