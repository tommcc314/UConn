#include <stdio.h>

int main(int argc, char* argv[]) {
	/* Caesar cipher
	 * take as input a sequence of characters from stdin using getchar
	 * ABCD
	 * take as command line input an integer 
	 * 3
	 * Cipher: shift the characters by the integer: ABCD(3) --> DEFG
	 * wrap around towards the end: Z --> C
	 * return as output a sequence of characters shifted by the integer on stdout using putchar
	 */
	return 0;
}
